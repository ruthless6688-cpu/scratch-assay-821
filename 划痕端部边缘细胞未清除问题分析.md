---
title: 划痕端部边缘细胞未清除问题分析
日期: 2026-08-21
场景: 真实划痕图 131516 掩膜清理环节 —— 红圈标出的细胞群在输出中未被清除
状态: 仅分析，未改动代码
---

# 划痕端部边缘细胞未清除问题分析

> 本文档记录一次针对真实输出结果的疑问排查过程：用户发现伤口上下端部的细胞群（图中红圈处）在掩膜清理后**没有被清除**，要求分析原因。全文为分析结论 + 验证证据，**未对代码做任何修改**。

---

## 1. 问题现象

- **左图（输入）**：原始分割叠加图，黄色 = 仪器软件标出的伤口掩膜，伤口内可见大量细胞（含端部细胞团、游离单细胞）。
- **右图（输出）**：项目流水线跑出的蓝色伤口掩膜叠加图。划痕中央的游离单细胞已被填成蓝色，但**上下端部红圈处的细胞群仍然保留（未被填充）**。

涉及图片：`Input/A1_11_12_20260228_131516_cellScratch.jpg`（8 张输入中的最后一张，伤口内细胞最多、残留缺口最多）。

---

## 2. 根因分析

### 2.1 流水线现状

掩膜清理的唯一动作在 `run_real_combined.py` 的 `_finalize()`：

```python
m = ndimage.binary_fill_holes(mask > 0)   # 只填充"完全被掩膜包围"的孔洞
m = 最大连通域
```

### 2.2 直接原因

`binary_fill_holes` 有个硬性条件：**该区域必须被掩膜完全包围**。红圈处的细胞群不满足：

- 顶部对角细胞链：从图像上边缘一直伸到伤口里，**连在图片边界上**；
- 左上角细胞簇：直接**挂在左侧细胞层上**；
- 底部细胞链：挂在右侧细胞层上 / 连到图像底边。

它们在掩膜里是"从背景伸进伤口的**半岛**"，和外面的大片细胞背景属于同一个连通域——`fill_holes` 认为它们是背景的一部分而非孔洞，因此不碰。

### 2.3 验证证据

| 验证项 | 结果 |
| --- | --- |
| 连通性诊断：顶部/底部未清除像素所属背景连通域 = 图像边缘背景连通域 | ✅ 确认连通（顶部、底部均为 True） |
| 视觉对照 | 划痕中央完全被黄色包围的游离单细胞，全部被填成蓝色；只有"挂边"的没填 |
| 包络版（41px 闭运算）效果 | 顶部未清除 73154→59603 px，底部 81042→71346 px，仍残留约 81% |

**实测残留细胞簇宽度（包络版处理后，顶部+底部区域）：**

| 残留位置 | 面积 | 最大宽度 | 是 41px 内核的 |
| --- | --- | --- | --- |
| 顶部边界挂链（y≈9~13） | 828~1230 px | 44~46 px | 1.1 倍 |
| 底部边界挂链（y≈2024~2034） | 1150~2818 px | 45~66 px | 1.1~1.6 倍 |
| 左侧/右侧大细胞团（红圈主体） | 30~35 万 px | 700~800 px | **17~20 倍** |

**关键数字：单细胞本身宽度约 40~46px。** 41px 内核恰好只够填"单细胞级别的边缘凹陷"（这是包络版设计时定 41 的由来），而红圈里的是**多细胞连片团**（宽 60~200px+，是内核的 2~5 倍以上）。

### 2.4 为什么 41px 包络闭运算也救不了

`clean_mask_envelope`（41px 椭圆闭运算）的设计意图正是"填平边缘细胞凹陷"，但它对端部细胞团不生效：

1. **内核尺度只对标单细胞**：闭运算只能填"宽度 < 41px 的凹陷"，多细胞团宽 60~200px+，数学上就填不掉；
2. **团连着层**：细胞团基底连着细胞层（属于背景的一部分），闭运算要切掉它需要内核 ≥ 团宽（17~20 倍）——那会把所有真实边缘一起抹平；
3. **边界挂链**：连到图像上下边缘的细胞链（宽 44~66px）也超过内核，且与边界背景连通。

结论：包络版不是"没处理"，是**处理级别只到单个细胞**，而端部这些需要的是"细胞团"级别的判定。

### 2.5 生物学含义

这些挂边细胞群就是交接文档中定义的**半脱离细胞**（以细颈与主体细胞层相连的半岛状细胞簇）。从掩膜角度它们该被清除（伤口应为连续空白带）；从生物学角度它们属于细胞，是划痕制作的残留物——设计文档的结论是**识别 + 告警（占比 > 15%），可选边界修正回填**，而不是无脑填充。

另有一个设计张力：现有 `fill_holes` 把划痕中央的**游离细胞也一律填掉**——如果以后要做半脱离细胞统计，这个操作会提前销毁证据（好在设计里规定用"未闭运算的原始 Mask"做识别，原始数据还在）。

---

## 3. 可选的解决方案（已分析，未实施）

### 3.1 方案对比

| 方案 | 原理 | 效果 | 副作用 |
| --- | --- | --- | --- |
| 现有 fill_holes | 只填完全封闭的孔洞 | 半岛全漏（现状） | — |
| 大核闭运算（41→101px+） | 填宽度 < 内核的凹陷 | 能填一部分 | 把真实边缘形态抹平，宽度统计/前沿粗糙度失真 |
| **包围度射线法** | 对背景像素向 N=16 方向发射线，统计命中伤口掩膜的方向占比，≥ 阈值（如 0.75）判为半岛 → 填充 | 精准命中"大部分被包围"的团 | 需调阈值；对"挂在层上但只深入浅"的边缘细胞不生效 |
| **侵入深度 vs 连接宽度判据**（半脱离三条件） | 连接颈宽 < 自身最大宽 1/3、侵入深度 > 自身宽 1/2 → 回填 | 符合设计文档，与粗糙度指标不冲突 | 需逐团分类，工作量中等 |
| **分区处理**（端部带 + 中部带） | 上下端部带用新方法（大核闭运算/包围度），中部两侧保留现有流程 | 贴合用户意图：只动端部，两侧原样 | 带边界有形态接缝；带内细胞层本体需防误填 |

### 3.2 分区处理框架（推荐方向）

```
raw mask（黄色提取）
   ├─ 端部带 y < h_end 或 y > H − h_end（h_end 可配置，如 250px）
   │      → 新方法：带内大核闭运算/包围度判定 + 回填
   ├─ 中部带
   │      → 现有流程：41px 闭运算 + fill_holes + 最大连通域（原样保留）
   └─ 两段结果拼合 → 最大连通域
```

**实施时必须注意的三个坑：**

1. **端部带里也包含两侧细胞层本体**（宽 700~800px）——新方法必须限定作用域：只对"位于伤口横向范围内、且与伤口相邻/被伤口包围"的背景团生效，细胞层本体不碰；
2. **跨界细胞团按"整团"处理**——以连通团为单位，团与带相交 → 整团按新方法判定，避免半截形状；
3. **带边界接缝**——带内边缘平滑、带外粗糙，分界线处形态突变。可接受则用位置分区；不可接受则改用连通性判据（连到图像上下边界的团 → 新方法；连到左右细胞层的团 → 保留现状），但注意连通性方案不会填掉"挂在左侧层上但位于端部"的团。

---

## 4. 待用户拍板的参数

1. **端部带范围 h_end**：默认建议 250px（红圈内的链和团都在距边界 ~250px 以内）；
2. **接缝容忍**：位置分区 vs 连通性分区；
3. **处理目标**：A 一刀切（挂边细胞团一律回填）还是 B 按深度切（只回填侵入伤口达到一定深度的部分，保留边缘天然起伏）。

---

## 5. 涉及的代码位置（参考）

| 文件 | 位置 | 说明 |
| --- | --- | --- |
| `run_real_combined.py` | `_finalize()`（约 L124-130） | 孔洞填充 + 最大连通域——根因所在 |
| `run_real_combined.py` | `clean_mask_envelope()`（L133 起） | 41px 椭圆闭运算，意图"填平边缘细胞凹陷"，对细胞团不生效 |
| `scratch_width_proto.py` | 全库 | 中轴测宽，未涉及掩膜清理 |
| `qc_metrics.py` | `evaluate_width_quality()` / `front_roughness()` | 质控指标，与掩膜清理解耦，未受影响 |

*分析完毕，等待用户确认参数后再决定是否实施。*
